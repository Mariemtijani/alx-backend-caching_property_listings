version: '3'

services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: propertydb
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password

  redis:
    image: redis:latest
    ports:
      - "6379:6379"
